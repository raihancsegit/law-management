'use client';
export default function SecurityTab({ profile, setProfile }: any) {
    return (
        <div id="security-section" className="profile-section hidden">
            <h3 className="text-lg font-semibold text-gray-900 mb-4">Security Settings</h3>
            {/* ... আপনার SecurityTab-এর HTML এখানে যোগ করুন ... */}
        </div>
    );
}